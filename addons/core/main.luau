return {
    -- Called when the mod is loaded
    init = function ()
        -- Engine.log_info("INIT CALLED")
    end,

    post_init = function ()
        -- Engine.log_info("POST INIT CALLED")
    end,

    unload = function ()
        -- Engine.log_info("UNLOAD CALLED")
    end,

    load = function ()
        -- Engine.log_info("LOAD CALLED")
    end,
}
